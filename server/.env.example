# PostgreSQL connection string
DATABASE_URL="postgresql://doremi:doremi_dev@localhost:5432/doremi?schema=public"

# API server port
PORT=4000

NODE_ENV=development

# Origin allowed for CORS (the Next.js app)
CORS_ORIGIN=http://localhost:3000

# JWT signing secrets — generate strong random values for production
JWT_ACCESS_SECRET=dev_access_secret_change_me_in_production
JWT_REFRESH_SECRET=dev_refresh_secret_change_me_in_production
